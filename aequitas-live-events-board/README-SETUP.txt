AEQUITAS LIVE EVENTS BOARD
Upload index.html, schema.sql, and the functions folder to GitHub. Connect the repo to Cloudflare Pages. Create a D1 database, run schema.sql, bind it as DB, and add encrypted secrets ADMIN_CODE and PRIVATE_EVENT_CODE. Never put the codes in index.html.
